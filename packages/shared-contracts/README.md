# shared-contracts

只保存稳定的跨语言协议，不是 REST DTO 的主流程。

- `errors/error-codes.json`：错误码
- `enums/`：公共枚举
- `schemas/sse-events.schema.json`：SSE 事件 Schema
- `feature-flags/`：Feature Flag Key
- `openapi/openapi.json`：由后端 Pydantic 导出的版本化契约产物

> 当前枚举为 P0 占位值，进入对应阶段后以 V2.0 方案最终确认为准。
